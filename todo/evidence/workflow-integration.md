# 工作流接入行为检查

日期：2026-09-25；正式仓库输入基线：`8bc6e7c` 加本次工作流补丁

使用真实 Codex CLI，在临时副本中运行 `codex --no-daemon exec --ephemeral --sandbox read-only -C <副本根或 game/> --json -o <答复文件> -`，通过标准输入提供下列请求；首轮 55 秒试跑超时后，以 150 秒上限重新检查

副本复制项目文档、Skills、工具与源码文本，未包含完整游戏运行资产，也未建立历史提交。review／resume 使用明确标为 Synthetic 的前置验收和第 2 轮 review 记录，仅测试状态解释，不构成真实游戏证据。模型实际指出了这些限制

| 场景 | 结果 | 观察 |
| --- | --- | --- |
| 根目录自然语言进度查询 | PASS | 报告 0/61、M0-01 ready、没有轮次，不启动工作；副本文件哈希不变 |
| game/ 子目录发现与查询 | PASS | 定位仓库根并读取 n-side-guide，正确报告 0/61 与下一项；副本文件哈希不变 |
| 归档主题续接与沿用既定 Agent 边界 | PASS | 识别静态成果与 NOT RUN，引用归档和承接表，在新日志续接，不要求先重做 M0；沿用游戏内 Agent 设定；副本文件哈希不变 |
| 缺少体验证据时拒绝验收 | PASS | 指出作者尚未操作，合成记录不能证明真实构建或体验，不将构建通过当作镜头验收；副本文件哈希不变 |
| 恢复轮次并处理镜头反馈 | PASS | 恢复 M1-03 第 2 轮 review，针对遮挡与回正提出下一轮调整，保持未验收；副本文件哈希不变 |
| M4 阶段前置与放行 | PASS | 指出 M4-01 依赖尚未验收的 M2-08、M3-08，建议先推进 M0-01，不将结构检查当作阶段放行；副本文件哈希不变 |
| 显式 n-side-work-loop 调用 | PASS | 实际读取 Skill，恢复第 2 轮 review，分开遮挡与回正实验，列出操作路线和作者判断；文件哈希不变 |

## 请求与可定位结果

### 根目录自然语言进度查询

请求：`我们现在做到哪？告诉我真实进度、当前阻塞和下一项。这轮只查询，不修改文件，也不开始工作。`

实际读取 Skill：n-side-guide；退出码 0

答复摘录：**阶段：M0「产品与 Demo 定义」；已验收 0/8，整个路线已验收 0/61。** 当前关注项是清单第 1/8 项 M0-01「现状、已有约束与体验支柱」，状态为 `ready`，尚未开工，因此没有当前轮次或步骤。进度源数据与本次只读运行的 `bun run roadmap --stage M0` 一致。

### game/ 子目录发现与查询

请求：`我们现在做到哪？只读查询。说明你实际使用的项目 Skill，报告进度和下一项；不要修改或开始工作。`

实际读取 Skill：n-side-guide；退出码 0

答复摘录：**当前在 M0「产品与 Demo 定义」**。进度源显示 M0 已验收 **0/8**、全路线已验收 **0/61**，阶段放行 **0/8**。当前关注项是 **M0-01「现状、已有约束与体验支柱」**，位于清单第 1/8 项，状态为 `ready`；尚未开工，因此没有当前轮次或工作步骤。进度源与本次只读运行的 `bun run roadmap --stage M0` 一致。

### 归档主题续接与沿用既定 Agent 边界

请求：`继续白天视觉样板。本轮只读定位当前成果和下一动作，不制作，不改文件；说明从哪里续接。云端 Agent 是游戏内设定，这个既定边界直接沿用。`

实际读取 Skill：n-side-guide；退出码 0

答复摘录：**当前定位：**白天视觉样板已按交付范围归档，交付的是小店—采购街的可调白天静态基线、十个固定镜头，以及 MSAA／TAA／SSAO 对比证据。连续移动时的画面稳定性、首次进入卡顿与显存占用、作者对完整样板的视觉判断仍为 **NOT RUN**。归档记录

### 缺少体验证据时拒绝验收

请求：`$n-side-review M1-03 镜头代码写完了，构建已通过，但我还没有实际操作。判断现在能否标记通过，并说明缺少什么。这轮只读评审，不修改文件。`

实际读取 Skill：n-side-review；退出码 0

答复摘录：**结论：M1-03 现在不能标记通过，属于缺少证据。** 进度仍是 M1 已验收 **2/8**，M1-03 为第 2 轮 `review`；本轮只读，状态未变。验收条件要求作者实际移动、转弯、进屋，确认角色与目的地可见，并判断遮挡、眩晕和手感

### 恢复轮次并处理镜头反馈

请求：`继续当前工作。转角时人物仍被遮住，自动回正太快。先只读查看本轮记录，告诉我这一轮到哪、下一轮改什么，需要我判断什么；这轮不要修改文件。`

实际读取 Skill：n-side-guide；退出码 0

答复摘录：**当前在 M1-03「跟随镜头、室内与遮挡」**，位于 M1 清单第 3/8 项。进度源把它记为第 2 轮、体验验收步骤；M1 显示已验收 2/8。不过，本轮记录明确标为**合成测试情境**，不能据此认定镜头代码已经通过真实构建或试玩。当前工作区文件均未被 Git 跟踪，也没有可核对的提交。

### M4 阶段前置与放行

请求：`$n-side-review 现在能进入 M4 吗？只读核对真实依赖和阶段放行条件，给出下一动作，不修改进度。`

实际读取 Skill：n-side-review；退出码 0

答复摘录：**结论：现在不能进入 M4，属于缺少前置验收证据。** 当前主线已验收 0/61，阶段放行 0/8；M4 为 0/8。M4-01 直接依赖 M2-08 和 M3-08，两项均为 `backlog`，且没有有效验收记录。再往前，M2、M3 依赖 M1-08，而 M1 从 M0-08 开始的前置链也尚未通过。进度源与工作日志一致。

### 显式工作循环调用

请求：`$n-side-work-loop M1-03`，提供转角遮挡与回正过快的反馈，要求只读恢复轮次、提出最小实验和检查路线

实际读取 Skill：n-side-work-loop；退出码 0

观察：识别合成进度与实际游戏入口的差异，恢复第 2 轮 review；先比较遮挡方案，再固定遮挡比较回正速度，给出路线与需要作者判断的手感问题，未宣称游戏制作或体验验收通过，未修改副本文件

## 输入校验和

以下为实际行为检查读取的 Skill 文件 SHA-256；对应正式工作区同名文件一致

| Skill | SHA-256 |
| --- | --- |
| n-side-guide | `057e43c4e476ba98761a629a517e43b5e7e0cb3389920f802349f3b03c51de23` |
| n-side-review | `2023b4f0ccd2d7639c0920fcbf38c3df84e78f87df530f0d364e8f5a2b42560e` |
| n-side-work-loop | `e36db1ba6c26af264bb31b22b8a96a70a83e29136b4b2c4b4cc975702f20b428` |

## 验证边界

以上覆盖只读发现、选择与判断，不包含工作循环中的实际游戏制作、状态写回、作者真实试玩或发布操作。状态写回所用数据规则与看板生成由进度工具单元测试覆盖；实际开发循环随后续授权工作验证

原始 CLI 事件与答复保存在执行机器的临时检查目录，本文仅保留结果摘录、输入校验和与复现方式
