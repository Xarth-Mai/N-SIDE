# UI 制作与交互验收

专业方法使用 `game-ui-ux`，具体布局、交互和焦点 API 使用与锁文件匹配的 `bevy-ui`。本页规定 N:SIDE 的页面制作与验收方法；具体页面信息和动作归对应系统规格，不另建一套任务或世界状态

当前正式游戏入口展示 Logo，Viewer 的自由相机输入也不是菜单系统。以下菜单、对话与调查界面是制作契约，不能据此宣称暂停、手柄导航或调查记录已经实现

## 输入与前提

先取得页面用途、所属玩家动作、信息来源、进入条件、可用输入和退出目的地。调查页面只显示玩家已观察的事实、获得的线索与当前推测；世界真相与剧情谜底按委托的信息顺序出现

从已确认的 Demo 范围选择本轮页面，先制作能实际走通的少量页面和状态。视觉风格、图标与文字层级沿用[美术方向](art-direction.md)；实际字体、分辨率、缩放档位与控制映射记录在本轮规格，尚未验证的参数不写成统一阈值

## 从流程到接入

1. 写出进入 → 阅读或选择 → 确认或取消 → 返回的短流程，列出正常、空、加载、不可用、错误状态；不适用的状态注明原因
2. 确定每层界面的输入所有者、默认焦点、导航顺序和返回目标；弹窗阻止操作落到后层，关闭后恢复仍有效的原焦点，否则回到明确的可用默认项
3. 使用容器、相对布局和实际文字试排，处理长标题、多行对白、列表滚动与宽高比；焦点、选中、悬停、按下和禁用各有清楚表现，必要状态不只靠颜色区分
4. 接入真实状态与动作请求：界面读取所属系统提供的信息，确认后由系统执行规则与结果，成功或失败再反馈到界面；使用 Bevy 变更检测或既有消息，不因上游引擎示例新建通用事件总线
5. 在输入路由中区分页面导航与游戏动作，验证同一按键不会既关闭菜单又触发场景动作；设备切换更新提示，不清空仍有效的焦点和选择
6. 先验证状态和导航，再在真实画面检查文字、遮挡与操作反馈；修复后重跑受影响的进入、返回与恢复路线

## 导航与恢复约定

| 情况 | 必须明确的行为 | 检查方式 |
| --- | --- | --- |
| 打开页面或列表 | 有可见默认焦点；空列表仍有返回入口 | 键鼠和手柄分别从未操作状态进入 |
| 打开子页或确认框 | 上层独占导航，底层暂停接收同类输入 | 持续移动、确认、取消，检查底层状态没有误变 |
| 关闭子页 | 回到仍存在的先前控件；目标已删除时用默认项 | 删除、隐藏或禁用先前目标后返回 |
| 鼠标与手柄切换 | 悬停和焦点可区分，最后有效选择可继续使用 | 在同一页面往返切换并确认 |
| 断连、失焦与恢复 | 无持续误输入，恢复路径可达，提示与实际设备一致 | 真实拔插或失焦；模拟结果单独说明 |
| 暂停、切场、读档 | 显式选择关闭还是重建页面；不保留旧实体引用 | 打开页面后中断并恢复，检查事实与焦点一致 |
| 失败与重复提交 | 明确失败原因及重试方式；一次动作只提交一次 | 快速确认、返回、重入和恢复后的重复操作 |

暂停是否冻结世界、对话能否跳过、关闭时是否保存选择由系统规格决定；页面实现不能自行改变任务规则。设置页面只有实际生效且可恢复的选项才作为可用项交付

## 输出、命令与失败处理

交付流程与状态表、实际页面及组件、双输入路线、机器结果和[视觉自查](../validation/visual-review.md)。共用规则回写本页或系统规格，本轮分辨率、设备和截图记录归任务证据

从仓库根执行以下 fish 命令可检查现有工程；它们不能代替尚未接入的界面运行

```fish
cargo fmt --manifest-path game/Cargo.toml --check
cargo test --manifest-path game/Cargo.toml --features viewer --locked
cargo run --manifest-path game/Cargo.toml --locked
```

接入首个界面后，沿[运行验证](../validation/runtime.md)扩展真实输入与状态路径，不单独写“看起来像界面”的录制副本。机器检查焦点可达、输入隔离、可用状态和提交次数；画面检查字体、对比、遮挡与反馈，实际操作检查确认、取消与设备切换

若任一输入无法返回，先修路由或焦点再评美术；若信息超前，修正其状态来源，不以隐藏颜色掩盖。素材加载失败保留可读的错误与返回路径，不能留下只可用鼠标绕出的空白页面。缺少真实手柄或目标平台时将对应项记为 NOT RUN

## 来源与适配

基于 [game-ui-ux](https://github.com/gamedev-skills/awesome-gamedev-agent-skills/tree/44888f28ff918357ad82c4473352c60a1c5bde5b/skills/disciplines/game-ui-ux)改编，固定 commit `44888f28ff918357ad82c4473352c60a1c5bde5b`，Copyright 2026 Abhishek Barali and the awesome-gamedev-agent-skills contributors，[Apache-2.0 LICENSE](https://github.com/gamedev-skills/awesome-gamedev-agent-skills/blob/44888f28ff918357ad82c4473352c60a1c5bde5b/LICENSE) · [NOTICE](https://github.com/gamedev-skills/awesome-gamedev-agent-skills/blob/44888f28ff918357ad82c4473352c60a1c5bde5b/NOTICE)

Modified for N:SIDE：将跨引擎布局、焦点与页面流程方法连接到 Bevy、双输入、调查信息边界和现有验收入口，未采用 Godot/Unity 代码或新增中间件。完整声明与文件对应关系见仓库根 `THIRD_PARTY_NOTICES.md` 与 `third_party/skills/awesome-gamedev-agent-skills/`
